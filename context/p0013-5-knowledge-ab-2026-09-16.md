# P0013.5 — Knowledge-Grounded Business Analysis: Replay A/B Artifact (2026-09-16)

**类型**: 真实验收产物（保留供 Operator 判断认知质量；Claude 不对业务质量下结论）
**A/B 是同一 frozen 数据集、同一窗口、同一 enrichment、真 Hermes 的两次完整回放。**

---

## 1. A/B 设置（两侧完全同构）

| | BEFORE | AFTER |
|---|---|---|
| run | `1bbc1239`（COMPLETED 11/11） | `993070af`（COMPLETED 11/11） |
| 窗口 | 2026-09-02 → 2026-09-12 | 同 |
| 数据集 | `data/jd_acquisition_20260914_0231` (manifest `87613e01…`) | 同 |
| Enrichment | 09-04 `action` 增加教师节优惠政策；09-10 `action` 调整教师节优惠政策 | 同（逐字相同） |
| Hermes session | `20260916_085242_406bf4` | `20260916_104130_14875b` |
| model / provider | grok-4.5 / custom（真 Hermes :9120） | 同 |
| 契约 | Replay prompt 仅含 `Knowledge ≠ Evidence` 禁止段 | **引入 shared `KNOWLEDGE_ANALYSIS_SECTION`** |

BEFORE 由 Operator 在本次改动之前跑出，未重跑（无法重跑：prompt 已变更）；BEFORE 证据取自其 Hermes session 原始记录。

---

## 2. Knowledge Available → Retrieved

**Available**（两侧相同）：`data/fabric-workspace/knowledge/` 26 个 .md
（operations 11 / product 5 / conversion 3 / traffic 2 / 顶层 5）＋ `knowledge-sources/raw/` 12 份。

**Retrieved（真实 Hermes tool call 记录）**

| | BEFORE | AFTER |
|---|---|---|
| 读取的 knowledge 文件数 | **0** | **5** |
| 具体文件 | — | `knowledge/INDEX.md`（x2）、`knowledge/operations/INDEX.md`、`knowledge/product/INDEX.md`、`knowledge/operations/true-vs-false-anomaly.md`（x2）、`knowledge/product/atp-drop-diagnosis.md` |
| 导航形态 | 无 | **INDEX → domain INDEX → 相关页**（三层，非 bulk） |
| 工具构成 | skill_view 7 / tool_describe 2 / tool_call 31 / terminal 1 | skill_view 7 / **read_file 7** / tool_describe 2 / tool_call 43 / terminal 1 / **search_files 6** / execute_code 2 |

**导航时序（AFTER，如实记录）**：Knowledge 导航集中在 **09-02 与 09-03 两天**；09-04 → 09-12 未再读取文件，后续日子的 `basis_refs` 引用的是本 run 内已读过的页（见 §3）。即：**index-first、按需、前置**，不是每日重复读取——符合"不 bulk、不要求固定页面"，但也**不是**"每天读一次"。

---

## 3. Knowledge Used（raw Hermes cognition 中的可追踪应用）

AFTER run 原始输出中，Knowledge 出现在 `thresholds[].provenance` / `basis_refs`（结构上唯一被允许的位置）：

```json
{ "statement": "单日指标波动 ±15% 内优先观察、不启动干预（知识先验；本判断不依赖该门，而依赖结构门禁）",
  "provenance": "knowledge_rule",
  "basis_refs": ["knowledge/operations/true-vs-false-anomaly.md"] }          // 2026-09-02
```
```json
{ "statement": "脉冲日后 GMV 下行若伴随 1000+ 归零与 n 收缩，优先结构解释，不自动故障树加投",
  "provenance": "knowledge_rule",
  "basis_refs": ["knowledge/operations/true-vs-false-anomaly.md"] }          // 2026-09-04
```

同句显式区分了"知识先验"与"本判断实际依赖"（`本判断不依赖该门`）。

**输出级计数对照**

| marker | BEFORE | AFTER |
|---|---|---|
| `provenance: "knowledge_rule"` | **0** | **3** |
| `knowledge/` 出现在输出中 | 9（**全部是 prompt 边界原文的回显**，非引用） | 25（`basis_refs` / `provenance`） |
| `知识先验` / `故障树` / `真伪异常` | 0 / 0 / 0 | 1 / 1 / 1 |

---

## 4. Knowledge ≠ Evidence 边界（无越权）

对 AFTER 全部 11 天原始输出做结构扫描：

- `observed[]` / `observed_facts` 层出现 Knowledge 文案的次数：**0**（无升格）
- Knowledge 引用仅出现在 `provenance` / `basis_refs`：**3/3**
- 因果纪律（Enrichment 动作 → 结果）实例：

```json
{ "claim": "优惠导致今日结构/GMV 变化",
  "evidence_refs": ["operator_action:2026-09-04"],
  "missing_evidence": ["核销、对照、分时"] }                                   // 记为待证 claim
```
```
"教师节优惠如何读？" → "动作在 09-04，结构高客单在 09-05，时间可衔接但无核销，禁止写成优惠导致高客单。"  → H3 保持 proposed
"礼盒名含中秋教师节不足以证明已报名活动或发券"                                  // operations 维度记为 gap
```

---

## 5. 留给 Operator 判断的部分

本 artifact 只报告 provenance 与结构事实。**"是否更像专业运营分析"由 Operator 判断**——Claude 不对认知质量下结论。

供对照的两侧逐日原始输出可从 Hermes 原始会话读取：

- BEFORE session `20260916_085242_406bf4`（`~/.hermes/state.db` messages）
- AFTER session `20260916_104130_14875b`（同上）

Workspace 中亦可直接对比：`历史回放` → run `1bbc1239` 与 run `993070af`。

---

## 6. 运行特征（非缺陷，如实记录）

AFTER run 的 09-09 步触发 ADR-085 已知的 Hermes compression（日志：`Context compression still streaming after 240s`），该步耗时约 7 分钟但最终 COMPLETED；11/11 全部完成，无 FAILED。该债务按任务边界未处理。
